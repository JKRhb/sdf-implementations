# Rust-Based Semantic Definition Format (SDF) Data Structures

This repository contains structs and (de)serialization logic for the Semantic
Definition Format (SDF, RFC 9880).
The data structures include SDF models, supplements, and messages.
Experimental support for an HTTP and a CoAP protocol mappings is also included.
