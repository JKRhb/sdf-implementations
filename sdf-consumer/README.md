# SDF Consumer

This crate contains the SDF Consumer, an experimental CLI tool for interacting with SDF-enabled Things based on instance-related messages and the associated models.

The protocols usable with this crate are currently limited, however, limited support for CoAP, (CoAPS), HTTP, and HTTPS is provided, at least when it comes to the reading propoerties and listing configuration parameters.
